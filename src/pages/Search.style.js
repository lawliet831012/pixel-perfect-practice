import makeStyles from '@mui/styles/makeStyles';

export default makeStyles(() => ({
  container: {
    paddingTop: 92,
    maxWidth: 850,
    margin: 'auto',
  },
  cardsContainer: {
    paddingTop: 24,
    justifyContent: 'center',
  },
}));
